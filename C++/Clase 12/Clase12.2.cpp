#include <iostream>
using namespace std;
//Dado un arreglo A de tamaño n, encontrar el mayor y el menor elemento de A.
int main()
{
	int a [n], bElement=0, sElement=0;
	for (int i=0;i<n;i++)
	{
		if (a[i]<sElement)
		{
			sElement=a[i]
		}
		if (a[i]>bElement)
		{
			bElement=a[i]
		}
	}
	
	return 0;
}